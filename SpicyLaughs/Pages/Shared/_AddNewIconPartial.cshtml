@model string

 @if (User.Identity.IsAuthenticated && User.IsInRole("admin"))
{ 
    <div style="position:fixed; right:30px; bottom: 50px;" class="text-white">
    <a asp-page="@Model" class="btn btn-success"><i class="bi bi-plus-circle"></i> Add New </a>
</div>
 } 